import 'package:flutter/material.dart';

void main() => runApp(MaterialApp(
      initialRoute: '/Lsoptions',
      routes: {
        // '/': (context) => Loading(),
        // '/login': (context) => Login(),
        // '/signin': (context) => SignIn(),
        // '/signup': (context) => SignUp(),
        // '/profile': (context) => Profile(),
        // '/info': (context) => Info(),
      },
    ));

